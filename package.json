{
  "name": "sephiria-solver",
  "version": "0.0.0",
  "private": true,
  "description": "Dev tooling only — the app itself is plain static files with no build step.",
  "scripts": {
    "lint:html": "html-validate --formatter stylish \"**/*.html\"",
    "lint:js": "eslint \"js/*.js\" \".github/scripts/*.js\"",
    "check:syntax": "find . -name \"*.js\" -not -path \"./node_modules/*\" -print0 | xargs -0 -n1 node --check",
    "check:links": "node .github/scripts/check-links.js",
    "check": "npm run lint:html && npm run check:syntax && npm run lint:js && npm run check:links"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "html-validate": "^9.7.1"
  }
}
